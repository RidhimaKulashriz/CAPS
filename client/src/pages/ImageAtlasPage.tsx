import { Image as ImageIcon } from "lucide-react";
import { Breadcrumb } from "@/components/SiteLayout";
const refs = [{ src: "/research/ml-lab.jpg", title: "Machine-learning research lab", source: "Public research image reference" }, { src: "/research/server-room.jpg", title: "Compute infrastructure", source: "Public research image reference" }, { src: "/research/whole-slide.jpg", title: "Whole-slide pathology scan", source: "Public pathology image reference" }];
export default function ImageAtlasPage() { return <main className="page-shell"><Breadcrumb current="Image atlas" /><div className="page-heading"><div><div className="eyebrow">IMAGE ATLAS</div><h1>Research imagery</h1><p>Real lab, compute, microscopy, and pathology references bundled into the CaPS workspace.</p></div><b className="big-count">20<small>references</small></b></div><div className="image-cards">{refs.map((item, i) => <article key={item.title} className={i === 0 ? "image-card feature" : "image-card"}><img src={item.src} alt={item.title} /><div><span>0{i + 1} / BUNDLED IMAGE</span><h2>{item.title}</h2><p>{item.source}</p><em className="internal-label">Available in this workspace</em></div></article>)}</div><div className="resource-rows image-rows">{["Microscopy and tissue references", "Machine-learning lab references", "Whole-slide image references", "Compute infrastructure references", "Semantic pathology visual set", "Research workflow imagery"].map((title, i) => <div className="catalog-row" key={title}><span>{String(i + 1).padStart(3, "0")}</span><b>{title}</b><small>Bundled CaPS catalog · no redirect</small><ImageIcon size={14} /></div>)}</div></main>; }
