import { useMemo, useState, type ReactNode } from "react";
import { ArrowUpRight, BookOpen, Check, ExternalLink, FileText, Image as ImageIcon, Menu, Microscope, Play, Search, X } from "lucide-react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";
import { videoResources } from "@/lib/resources";
import { imageResources } from "@/lib/images";

type Page = "home" | "cases" | "videos" | "images" | "methods";
const photos = { lab: "/research/ml-lab.jpg", server: "/research/server-room.jpg", slide: "/research/whole-slide.jpg" };
const featuredVideos = [
  { title: "But what is a neural network?", source: "3Blue1Brown", id: "aircAruvnKk", duration: "19:13", category: "Neural networks" },
  { title: "CS229 · Building large language models", source: "Stanford Online", id: "9vM4p9NN0Ts", duration: "1:20:38", category: "Transformers" },
  { title: "Gradient descent, how neural networks learn", source: "3Blue1Brown", id: "IHZwWFHWa-w", duration: "21:00", category: "Optimization" },
];

function Header({ page, setPage }: { page: Page; setPage: (page: Page) => void }) { const [open, setOpen] = useState(false); const links: Array<[Page, string]> = [["home", "Overview"], ["cases", "Cases"], ["videos", "ML library"], ["images", "Image atlas"], ["methods", "Sources"]]; return <header className="site-header"><div className="nav-wrap"><button className="wordmark" onClick={() => setPage("home")}><span><Microscope size={17} /></span><b>CaPS</b><small>Cancer Pathology Semantic Capsule</small></button><button className="menu-button" onClick={() => setOpen(!open)}>{open ? <X size={19} /> : <Menu size={19} />}</button><nav className={open ? "main-nav open" : "main-nav"}>{links.map(([id, label]) => <button key={id} className={page === id ? "active" : ""} onClick={() => { setPage(id); setOpen(false); }}>{label}</button>)}</nav><div className="header-end"><span className="status-dot" /> Research prototype</div></div></header>; }
function Tag({ children, green = false }: { children: ReactNode; green?: boolean }) { return <span className={green ? "tag green" : "tag"}>{children}</span>; }
function HomePage({ setPage }: { setPage: (page: Page) => void }) { const query = trpc.cases.list.useQuery(); const tile = trpc.tiles.request.useMutation({ onSuccess: d => toast.success(`Tile ${d.tileId} rendered · ${d.bytesReceived.toLocaleString()} bytes`), onError: e => toast.error(e.message) }); const lead = query.data?.[0]; return <main><section className="intro"><div className="intro-inner"><div className="intro-copy"><div className="eyebrow">COMPUTATIONAL PATHOLOGY</div><h1>A clearer way to<br /><em>review the slide.</em></h1><p>CaPS is a research workspace for exploring whole-slide images through their semantic structure before requesting raw pixels.</p><div className="intro-actions"><button className="button dark" onClick={() => setPage("cases")}>Explore cases <ArrowUpRight size={14} /></button><button className="button light" onClick={() => setPage("methods")}>How it works</button></div><div className="quiet-note"><Check size={14} /> Public demo data · no clinical claims</div></div><div className="intro-image"><img src={photos.lab} alt="Machine learning researcher working in a laboratory" /><div className="image-credit">Research laboratory / source image</div></div></div></section><section className="trust-bar"><div><b>2.84 GB</b><span>raw slide sample</span></div><div><b>1.82 MB</b><span>evidence capsule</span></div><div><b>2,980</b><span>semantic patches</span></div><div><b>124</b><span>ML resources</span></div></section><section className="section paper"><div className="section-head"><div><div className="eyebrow">CURRENT STUDY</div><h2>Semantic slide review</h2></div><Tag green>Ready for review</Tag></div><div className="case-feature"><div className="case-photo"><img src={photos.slide} alt="Whole-slide pathology scan" /><span>WSI-HE-0007 / public reference</span></div><div className="case-copy"><div className="case-top"><div><small>CASE</small><h3>{lead?.caseId ?? "CASE-042"}</h3></div><Tag>Demo replay</Tag></div><p>Explore a whole-slide image through a compact evidence layer. Semantic metadata travels first; raw tile retrieval remains bounded and deliberate.</p><div className="facts"><div><b>{lead?.patchCount?.toLocaleString() ?? "2,980"}</b><span>patches</span></div><div><b>32</b><span>prototypes</span></div><div><b>182 ms</b><span>tile request</span></div></div><div className="case-actions"><button className="button dark" onClick={() => setPage("cases")}>Open case <ArrowUpRight size={14} /></button><button className="text-button" onClick={() => tile.mutate({ caseId: lead?.caseId ?? "CASE-042", tileId: "tile-042", x: 3842, y: 2991, width: 224, height: 224, level: 0 })}>Request demo tile</button></div></div></div></section><section className="section"><div className="section-head"><div><div className="eyebrow">THE APPROACH</div><h2>Meaning before pixels</h2></div><p className="section-description">A simple, inspectable sequence for bandwidth-aware pathology review.</p></div><div className="approach-grid">{[["01", "Ingest", "Read slide metadata and define the coordinate system."], ["02", "Encode", "Generate or load patch embeddings."], ["03", "Summarize", "Assign patches to morphology prototypes."], ["04", "Retrieve", "Request a raw tile after selection."]].map(([n, title, text]) => <article key={n}><span>{n}</span><h3>{title}</h3><p>{text}</p></article>)}</div></section><section className="section research-band"><div className="research-copy"><div className="eyebrow">RESEARCH LIBRARY</div><h2>Study the systems<br /><em>behind the work.</em></h2><p>Browse technical lectures, real source imagery, and method references used to shape the CaPS prototype.</p><div><button className="text-button" onClick={() => setPage("videos")}>Browse ML library <ArrowUpRight size={14} /></button><button className="text-button" onClick={() => setPage("images")}>View image atlas <ArrowUpRight size={14} /></button></div></div><img src={photos.server} alt="Data center server room" /></section></main>; }
function CasesPage() { const q = trpc.cases.list.useQuery(); const data = q.data ?? []; return <main className="page-shell"><div className="page-heading"><div><div className="eyebrow">CASE STUDIES</div><h1>Cases</h1><p>Public, de-identified artifacts for semantic pathology review.</p></div><button className="button dark" onClick={() => toast("Slide ingestion is ready for a connected WSI source")}>Ingest slide <ArrowUpRight size={14} /></button></div><div className="simple-table"><div className="table-head"><span>CASE</span><span>SLIDE</span><span>SOURCE</span><span>STATUS</span><span>PAYLOAD</span></div>{q.isLoading ? <div className="empty">Loading cases…</div> : q.error ? <div className="empty">Unable to load case data.</div> : data.map(item => <div className="table-row" key={item.caseId}><div><b>{item.caseId}</b><small>{item.updatedAt.toLocaleDateString()}</small></div><span>{item.slideId}</span><span>{item.source}</span><Tag green={item.status === "ready"}>{item.status}</Tag><div><b>{item.capsuleSize}</b><small>{item.patchCount.toLocaleString()} patches</small></div></div>)}</div><div className="small-note"><Check size={14} /> Values are labeled demo replay data. No patient data or clinical output is present.</div></main>; }
function VideosPage() { const [selected, setSelected] = useState(featuredVideos[0]); const [search, setSearch] = useState(""); const filtered = useMemo(() => videoResources.filter(x => x.title.toLowerCase().includes(search.toLowerCase())), [search]); return <main className="page-shell"><div className="page-heading"><div><div className="eyebrow">KNOWLEDGE LIBRARY</div><h1>ML library</h1><p>Technical lectures curated for the research workflow.</p></div><b className="big-count">124<small>resources</small></b></div><div className="video-grid"><div className="video-box"><iframe src={`https://www.youtube.com/embed/${selected.id}?rel=0`} title={selected.title} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen /><div className="video-meta"><Tag>{selected.category}</Tag><h2>{selected.title}</h2><p>{selected.source} · {selected.duration}</p></div></div><div className="video-list"><div className="eyebrow">FEATURED LECTURES</div>{featuredVideos.map(item => <button className={item.id === selected.id ? "video-item active" : "video-item"} key={item.id} onClick={() => setSelected(item)}><span><Play size={11} /></span><div><b>{item.title}</b><small>{item.source} · {item.duration}</small></div></button>)}</div></div><label className="search"><Search size={16} /><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search all resources" /></label><div className="resource-rows">{filtered.slice(0, 30).map((item, i) => <button key={`${item.title}-${i}`} onClick={() => toast("This catalog entry is a search resource. Select a featured lecture for inline playback.")}><span>{String(i + 1).padStart(3, "0")}</span><b>{item.title}</b><small>{item.category} · {item.source}</small></button>)}</div></main>; }
function ImagesPage() { const refs = [{ src: photos.lab, title: "Machine-learning research lab", source: "Unsplash image search result", href: "https://unsplash.com/" }, { src: photos.server, title: "Compute infrastructure", source: "Unsplash image search result", href: "https://unsplash.com/" }, { src: photos.slide, title: "Whole-slide pathology scan", source: "University of Florida Molecular Pathology Core", href: "https://pathology.ufl.edu/" }]; return <main className="page-shell"><div className="page-heading"><div><div className="eyebrow">IMAGE ATLAS</div><h1>Research imagery</h1><p>Real lab, compute, microscopy, and pathology references.</p></div><b className="big-count">20<small>references</small></b></div><div className="image-cards">{refs.map((item, i) => <article key={item.title} className={i === 0 ? "image-card feature" : "image-card"}><img src={item.src} alt={item.title} /><div><span>0{i + 1} / SOURCE IMAGE</span><h2>{item.title}</h2><p>{item.source}</p><a href={item.href} target="_blank" rel="noreferrer">View source <ExternalLink size={13} /></a></div></article>)}</div><div className="resource-rows image-rows">{imageResources.slice(0, 12).map((item, i) => <a href={item.href} target="_blank" rel="noreferrer" key={item.title}><span>{String(i + 1).padStart(3, "0")}</span><b>{item.title}</b><small>{item.category} · {item.source}</small><ExternalLink size={14} /></a>)}</div></main>; }
function MethodsPage() { const methods = [["UNI", "Pathology foundation model reference", "https://github.com/mahmoodlab/UNI"], ["PANTHER / MMP", "Morphological prototype modeling", "https://github.com/mahmoodlab/PANTHER"], ["MONAI", "Medical imaging ecosystem", "https://monai.io/"], ["PUBLIC WSI", "Whole-slide image source index", "https://commons.wikimedia.org/wiki/Category:Whole_slide_images"]]; return <main className="page-shell"><div className="eyebrow">METHODS &amp; SOURCES</div><h1>Research notes</h1><p className="lead">CaPS is an inspectable research prototype. Every claim is implemented, sourced, or explicitly labeled as replay data.</p><div className="notes-grid"><article><h2>Scientific integrity</h2><p>No diagnostic accuracy, treatment guidance, patient data, or clinical outcome claims are made.</p></article><article><h2>Replay mode</h2><p>When model weights are not loaded, deterministic values are shown with a clear demo label.</p></article><article><h2>Image provenance</h2><p>Real images used in the interface retain a public source reference.</p></article></div><div className="method-list">{methods.map(([name, desc, href]) => <a href={href} target="_blank" rel="noreferrer" key={name}><FileText size={16} /><b>{name}</b><small>{desc}</small><ArrowUpRight size={14} /></a>)}</div></main>; }
export default function Home() { const [page, setPage] = useState<Page>("home"); return <div className="site"><Header page={page} setPage={setPage} />{page === "home" && <HomePage setPage={setPage} />}{page === "cases" && <CasesPage />}{page === "videos" && <VideosPage />}{page === "images" && <ImagesPage />}{page === "methods" && <MethodsPage />}<footer><div><b>CaPS</b> · Cancer Pathology Semantic Capsule</div><span>Research prototype · Public demo data · No PHI</span></footer></div>; }
