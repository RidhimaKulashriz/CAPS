import { FileText } from "lucide-react";
import { Breadcrumb } from "@/components/SiteLayout";
const methods = [["UNI", "Pathology foundation model reference"], ["PANTHER / MMP", "Morphological prototype modeling"], ["MONAI", "Medical imaging ecosystem"], ["PUBLIC WSI", "Whole-slide image source index"]];
export default function SourcesPage() { return <main className="page-shell"><Breadcrumb current="Sources" /><div className="eyebrow">METHODS &amp; SOURCES</div><h1>Research notes</h1><p className="lead">CaPS is an inspectable research prototype. Every claim is implemented, sourced, or explicitly labeled as replay data.</p><div className="notes-grid"><article><h2>Scientific integrity</h2><p>No diagnostic accuracy, treatment guidance, patient data, or clinical outcome claims are made.</p></article><article><h2>Replay mode</h2><p>When model weights are not loaded, deterministic values are shown with a clear demo label.</p></article><article><h2>Image provenance</h2><p>Real images used in the interface retain a public source reference in the catalog.</p></article></div><div className="method-list">{methods.map(([name, desc]) => <div className="method-row" key={name}><FileText size={16} /><b>{name}</b><small>{desc}</small><span className="internal-label">Reference</span></div>)}</div></main>; }
